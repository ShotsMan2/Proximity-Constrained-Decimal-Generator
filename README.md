# Proximity-Constrained-Decimal-Generator
A C-based stochastic algorithm that generates floating-point numbers within the [0.2, 1.2] range, enforcing a strict proximity constraint where the maximum deviation between any two data points must not exceed 0.1.
